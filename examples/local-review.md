# Local Review-First Example

```bash
clawcrew run "Review and implement a small auth improvement" --mode review --channel local
```

If the run pauses for approval:

```bash
clawcrew approve <approval-id>
```
